<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Final Score TextView -->
    <TextView
        android:id="@+id/text_final_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Final Score: 0"
        android:textSize="24sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"/>

    <!-- Name Input -->
    <EditText
        android:id="@+id/edit_player_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your name"
        android:layout_below="@id/text_final_score"
        android:layout_marginTop="16dp"/>

    <!-- Save Score Button -->
    <Button
        android:id="@+id/button_save_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Score"
        android:layout_below="@id/edit_player_name"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"/>

    <!-- View High Scores Button -->
    <Button
        android:id="@+id/button_view_high_scores"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View High Scores"
        android:layout_below="@id/button_save_score"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"/>

    <!-- Restart Game Button -->
    <Button
        android:id="@+id/button_restart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Restart Game"
        android:layout_below="@id/button_view_high_scores"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>